#! /usr/bin/env python
# encoding: utf-8
#
# Flare LEDs Driver
#

sources = []

board_sources = {
    'versal': [
        'empty-leds.c',
    ],
    'zynqmp': [
        'empty-leds.c',
    ],
    'zynq7000': [
        'zynq7000-leds.c',
    ]
}


def init(ctx):
    pass


def options(opt):
    pass


def configure(conf):
    pass


def build(bld):
    srcs = sources
    if bld.env.FLARE_BOARD in board_sources:
        srcs += board_sources[bld.env.FLARE_BOARD]
    bld.objects(target='flare_leds_driver', features='c', source=srcs)
